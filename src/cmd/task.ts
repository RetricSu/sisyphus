export async function customTask() {
  console.log('not impl');

  /*
  const message: Message = {
    role: 'user',
    content: 'content...',
  };
  const agent = new Agent({
    name: 'url-fetcher',
    memoId: 'url-fetcher',
    saveMemory: false,
    promptNames: ['read-url'],
    toolBoxes: [],
  });
  agent.LoadInitialMessages();
  const answer = await agent.call(message);
  console.log(answer.msg.content);
  */
}
